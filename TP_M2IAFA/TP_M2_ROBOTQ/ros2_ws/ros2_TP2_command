ros2 service list -t #liste des services et intefaces
ros2 service call /clear std_srvs/srv/Empty # exécution de la commande sur l'interface associée
ros2 run turtlesim turtle_teleop_key --ros-args --remap turtle1/cmd_vel:=turtle2/cmd_vel #remap 
ros2 topic list # la liste des topics existants turtle crées au TP précédants
ros2 service type # retourne le nom de l'interface
ros2 interface show std_srvs/srv/Empty # affiche l'état et le contenu de l'interface


Se placer sous workpackage/src 
ros2 pkg create --build-type ament_python py_pubsub
git clone https://github.com/ros2/examples.git

